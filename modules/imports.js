const express = require('express'); // required express
const mongoose=require("mongoose");// required mongoose
const dotenv=require("dotenv")
module.exports = { express, mongoose,dotenv }; // like react native 
